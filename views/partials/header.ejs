<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Members Only</title>
  <!-- google font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
  <!-- google icon -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
  <link rel="stylesheet" href="stylesheets/styles.css">
</head>
<body>
    <nav>
        <li class="logo"><a href="/"><span class="material-symbols-outlined">house</span>Secrets</a></li>
      <% if(currentUser) { %>
          <p class="account"><span class="material-symbols-outlined">account_circle</span> <%= currentUser.firstName %></p>
          <form class="log-out" action="/log-out" method="post">
              <button class="log-out" type="submit">Log Out</button>
          </form>
          <!-- <li><a href="/log-out">Log Out</a></li> -->
      <% }else{ %>
          <li><a href="/log-in">Log In</a></li>
          <li><a href="/sign-in">Sign In</a></li>
      <% } %>
    </nav>
